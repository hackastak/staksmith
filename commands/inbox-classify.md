---
description: "AI-powered classification of inbox items into PARA categories."
---

<!-- GENERATED by scripts/ci/generate-commands.js from the same-named skill. Safe to edit: the generator only creates missing commands and never overwrites. -->

# /inbox-classify

Invoke the **`inbox-classify`** skill and apply it to the request below. The full workflow, triggers, and output format live in `skills/inbox-classify/SKILL.md` — read it and follow it.

## Usage

```text
/inbox-classify [request or arguments]
```

$ARGUMENTS
