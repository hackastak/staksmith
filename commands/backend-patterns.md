---
description: "Backend architecture patterns, API design, database optimization, and server-side best practices for Node.js, Express, and Next.js API routes."
---

<!-- GENERATED by scripts/ci/generate-commands.js from the same-named skill. Safe to edit: the generator only creates missing commands and never overwrites. -->

# /backend-patterns

Invoke the **`backend-patterns`** skill and apply it to the request below. The full workflow, triggers, and output format live in `skills/backend-patterns/SKILL.md` — read it and follow it.

## Usage

```text
/backend-patterns [request or arguments]
```

$ARGUMENTS
