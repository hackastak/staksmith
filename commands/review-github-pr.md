---
description: "Review a GitHub pull request for the current branch by PR number. Performs read-only inspection across correctness, security, style, test coverage, architecture, and performance. Use when the user says things like 'review PR 186', 'review this pull request', or invokes '/review-github-pr <number>'."
---

<!-- GENERATED by scripts/ci/generate-commands.js from the same-named skill. Safe to edit: the generator only creates missing commands and never overwrites. -->

# /review-github-pr

Invoke the **`review-github-pr`** skill and apply it to the request below. The full workflow, triggers, and output format live in `skills/review-github-pr/SKILL.md` — read it and follow it.

## Usage

```text
/review-github-pr [request or arguments]
```

$ARGUMENTS
