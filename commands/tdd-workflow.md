---
description: "Use this skill when writing new features, fixing bugs, or refactoring code. Enforces test-driven development with 80%+ coverage including unit, integration, and E2E tests."
---

<!-- GENERATED by scripts/ci/generate-commands.js from the same-named skill. Safe to edit: the generator only creates missing commands and never overwrites. -->

# /tdd-workflow

Invoke the **`tdd-workflow`** skill and apply it to the request below. The full workflow, triggers, and output format live in `skills/tdd-workflow/SKILL.md` — read it and follow it.

## Usage

```text
/tdd-workflow [request or arguments]
```

$ARGUMENTS
