---
description: "Review backlog and todo notes across the Obsidian vault, verify whether open items are actually already done using evidence from the associated code repos and other notes, and mark the confirmed ones complete. Review only — never writes code or does the work. Use to reconcile stale backlogs with reality."
---

<!-- GENERATED by scripts/ci/generate-commands.js from the same-named skill. Safe to edit: the generator only creates missing commands and never overwrites. -->

# /backlog-review

Invoke the **`backlog-review`** skill and apply it to the request below. The full workflow, triggers, and output format live in `skills/backlog-review/SKILL.md` — read it and follow it.

## Usage

```text
/backlog-review [request or arguments]
```

$ARGUMENTS
