---
description: "Scan a codebase for deepening opportunities, present them as a visual Markdown report in the vault, then grill through whichever one you pick. Use when the user wants an architecture review, asks where the codebase is getting hard to change, or invokes /improve-codebase-architecture."
---

<!-- GENERATED by scripts/ci/generate-commands.js from the same-named skill. Safe to edit: the generator only creates missing commands and never overwrites. -->

Load the skill defined in `skills/improve-codebase-architecture/SKILL.md`, then follow its workflow to handle the request below. Treat that file as the authoritative instructions for this command.

$ARGUMENTS
