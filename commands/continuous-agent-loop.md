---
description: "Patterns for continuous autonomous agent loops with quality gates, evals, and recovery controls."
---

<!-- GENERATED by scripts/ci/generate-commands.js from the same-named skill. Safe to edit: the generator only creates missing commands and never overwrites. -->

# /continuous-agent-loop

Invoke the **`continuous-agent-loop`** skill and apply it to the request below. The full workflow, triggers, and output format live in `skills/continuous-agent-loop/SKILL.md` — read it and follow it.

## Usage

```text
/continuous-agent-loop [request or arguments]
```

$ARGUMENTS
