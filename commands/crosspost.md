---
description: "Multi-platform content distribution across X, LinkedIn, Threads, and Bluesky. Adapts content per platform using content-engine patterns. Never posts identical content cross-platform. Use when the user wants to distribute content across social platforms."
---

<!-- GENERATED by scripts/ci/generate-commands.js from the same-named skill. Safe to edit: the generator only creates missing commands and never overwrites. -->

# /crosspost

Invoke the **`crosspost`** skill and apply it to the request below. The full workflow, triggers, and output format live in `skills/crosspost/SKILL.md` — read it and follow it.

## Usage

```text
/crosspost [request or arguments]
```

$ARGUMENTS
