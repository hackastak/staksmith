---
description: "Wire the vault graph — find orphans, missing connections, and cluster bridges, score them, and add links/stubs. Connects notes without generating content. Optional cluster focus."
---

<!-- GENERATED by scripts/ci/generate-commands.js from the same-named skill. Safe to edit: the generator only creates missing commands and never overwrites. -->

# /backlinks

Invoke the **`backlinks`** skill and apply it to the request below. The full workflow, triggers, and output format live in `skills/backlinks/SKILL.md` — read it and follow it.

## Usage

```text
/backlinks [request or arguments]
```

$ARGUMENTS
