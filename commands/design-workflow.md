---
description: "Grill the user about the recurring loops in their life and work, and turn each one worth delegating into a workflow spec. Use when the user wants to design an automation, says \"help me spec a workflow\", or invokes /design-workflow."
---

<!-- GENERATED by scripts/ci/generate-commands.js from the same-named skill. Safe to edit: the generator only creates missing commands and never overwrites. -->

# /design-workflow

Invoke the **`design-workflow`** skill and apply it to the request below. The full workflow, triggers, and output format live in `skills/design-workflow/SKILL.md` — read it and follow it.

## Usage

```text
/design-workflow [request or arguments]
```

$ARGUMENTS
