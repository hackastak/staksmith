---
description: "Track how a specific idea has evolved across the Obsidian vault over time — find all mentions, follow wikilinks, and build a chronological timeline with connections. Use to see the history of a concept."
---

<!-- GENERATED by scripts/ci/generate-commands.js from the same-named skill. Safe to edit: the generator only creates missing commands and never overwrites. -->

# /trace

Invoke the **`trace`** skill and apply it to the request below. The full workflow, triggers, and output format live in `skills/trace/SKILL.md` — read it and follow it.

## Usage

```text
/trace [request or arguments]
```

$ARGUMENTS
