---
description: "C++ coding standards based on the C++ Core Guidelines (isocpp.github.io). Use when writing, reviewing, or refactoring C++ code to enforce modern, safe, and idiomatic practices."
---

<!-- GENERATED by scripts/ci/generate-commands.js from the same-named skill. Safe to edit: the generator only creates missing commands and never overwrites. -->

# /cpp-coding-standards

Invoke the **`cpp-coding-standards`** skill and apply it to the request below. The full workflow, triggers, and output format live in `skills/cpp-coding-standards/SKILL.md` — read it and follow it.

## Usage

```text
/cpp-coding-standards [request or arguments]
```

$ARGUMENTS
