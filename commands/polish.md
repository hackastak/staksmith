---
description: "Polish a blog draft against The HackaStak style guidelines — voice/structure/SEO audit, apply fixes, and generate a pre-publish checklist with meta description. Use after drafting, before scheduling."
---

<!-- GENERATED by scripts/ci/generate-commands.js from the same-named skill. Safe to edit: the generator only creates missing commands and never overwrites. -->

# /polish

Invoke the **`polish`** skill and apply it to the request below. The full workflow, triggers, and output format live in `skills/polish/SKILL.md` — read it and follow it.

## Usage

```text
/polish [request or arguments]
```

$ARGUMENTS
