---
description: "Swift 6.2 Approachable Concurrency — single-threaded by default, @concurrent for explicit background offloading, isolated conformances for main actor types."
---

<!-- GENERATED by scripts/ci/generate-commands.js from the same-named skill. Safe to edit: the generator only creates missing commands and never overwrites. -->

# /swift-concurrency-6-2

Invoke the **`swift-concurrency-6-2`** skill and apply it to the request below. The full workflow, triggers, and output format live in `skills/swift-concurrency-6-2/SKILL.md` — read it and follow it.

## Usage

```text
/swift-concurrency-6-2 [request or arguments]
```

$ARGUMENTS
