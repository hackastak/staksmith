---
description: "The house standard for Architecture Decision Records — when a decision earns an ADR, the mandatory sections, and the supersede-don't-edit rule. Use when writing, updating, or superseding an ADR, when deciding whether a decision is worth recording, or when another skill needs the ADR format."
---

<!-- GENERATED by scripts/ci/generate-commands.js from the same-named skill. Safe to edit: the generator only creates missing commands and never overwrites. -->

Load the skill defined in `skills/adr-standard/SKILL.md`, then follow its workflow to handle the request below. Treat that file as the authoritative instructions for this command.

$ARGUMENTS
