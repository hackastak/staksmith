---
description: "Manage the \"Stories You Won't Believe\" TikTok content calendar — buffer health, pillar balance, scheduling to the Mon/Wed/Fri cadence, and publish tracking via script frontmatter. Use to see pipeline status or move scripts through the pipeline."
---

<!-- GENERATED by scripts/ci/generate-commands.js from the same-named skill. Safe to edit: the generator only creates missing commands and never overwrites. -->

# /story-pipeline

Invoke the **`story-pipeline`** skill and apply it to the request below. The full workflow, triggers, and output format live in `skills/story-pipeline/SKILL.md` — read it and follow it.

## Usage

```text
/story-pipeline [request or arguments]
```

$ARGUMENTS
