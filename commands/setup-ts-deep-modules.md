---
description: "Wire dependency-cruiser into a TypeScript repo so each package is a deep module — implementation hidden in subfolders, reachable only through its entry-point files. User-invoked."
---

<!-- GENERATED by scripts/ci/generate-commands.js from the same-named skill. Safe to edit: the generator only creates missing commands and never overwrites. -->

# /setup-ts-deep-modules

Invoke the **`setup-ts-deep-modules`** skill and apply it to the request below. The full workflow, triggers, and output format live in `skills/setup-ts-deep-modules/SKILL.md` — read it and follow it.

## Usage

```text
/setup-ts-deep-modules [request or arguments]
```

$ARGUMENTS
