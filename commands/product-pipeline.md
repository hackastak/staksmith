---
description: "Manage product calendar and launches - view pipeline status, schedule releases, track revenue, and get recommendations on what to work on next."
---

<!-- GENERATED by scripts/ci/generate-commands.js from the same-named skill. Safe to edit: the generator only creates missing commands and never overwrites. -->

# /product-pipeline

Invoke the **`product-pipeline`** skill and apply it to the request below. The full workflow, triggers, and output format live in `skills/product-pipeline/SKILL.md` — read it and follow it.

## Usage

```text
/product-pipeline [request or arguments]
```

$ARGUMENTS
