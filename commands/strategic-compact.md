---
description: "Suggests manual context compaction at logical intervals to preserve context through task phases rather than arbitrary auto-compaction."
---

<!-- GENERATED by scripts/ci/generate-commands.js from the same-named skill. Safe to edit: the generator only creates missing commands and never overwrites. -->

# /strategic-compact

Invoke the **`strategic-compact`** skill and apply it to the request below. The full workflow, triggers, and output format live in `skills/strategic-compact/SKILL.md` — read it and follow it.

## Usage

```text
/strategic-compact [request or arguments]
```

$ARGUMENTS
