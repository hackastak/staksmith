---
description: "Set up a Claude Code hook that blocks dangerous git commands (push, reset --hard, clean -f, branch -D, checkout .) before they execute. Use when the user wants to prevent destructive git operations or add git safety hooks."
---

<!-- GENERATED by scripts/ci/generate-commands.js from the same-named skill. Safe to edit: the generator only creates missing commands and never overwrites. -->

# /git-guardrails

Invoke the **`git-guardrails`** skill and apply it to the request below. The full workflow, triggers, and output format live in `skills/git-guardrails/SKILL.md` — read it and follow it.

## Usage

```text
/git-guardrails [request or arguments]
```

$ARGUMENTS
