---
description: "Convert Obsidian vault project notes into architectural decisions, specifications, and CLAUDE.md files in code repositories."
---

<!-- GENERATED by scripts/ci/generate-commands.js from the same-named skill. Safe to edit: the generator only creates missing commands and never overwrites. -->

# /vault-to-code-bridge

Invoke the **`vault-to-code-bridge`** skill and apply it to the request below. The full workflow, triggers, and output format live in `skills/vault-to-code-bridge/SKILL.md` — read it and follow it.

## Usage

```text
/vault-to-code-bridge [request or arguments]
```

$ARGUMENTS
