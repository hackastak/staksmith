---
description: "Use only when writing/updating/fixing C++ tests, configuring GoogleTest/CTest, diagnosing failing or flaky tests, or adding coverage/sanitizers."
---

<!-- GENERATED by scripts/ci/generate-commands.js from the same-named skill. Safe to edit: the generator only creates missing commands and never overwrites. -->

# /cpp-testing

Invoke the **`cpp-testing`** skill and apply it to the request below. The full workflow, triggers, and output format live in `skills/cpp-testing/SKILL.md` — read it and follow it.

## Usage

```text
/cpp-testing [request or arguments]
```

$ARGUMENTS
