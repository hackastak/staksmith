---
description: "Review all the code on the current branch — the whole state of what this branch has built, read broadly and architecturally. Read-only. Use before pushing or opening a PR, or when the user says 'review this branch', 'review my work', or invokes '/code-review'."
---

<!-- GENERATED by scripts/ci/generate-commands.js from the same-named skill. Safe to edit: the generator only creates missing commands and never overwrites. -->

# /code-review

Invoke the **`code-review`** skill and apply it to the request below. The full workflow, triggers, and output format live in `skills/code-review/SKILL.md` — read it and follow it.

## Usage

```text
/code-review [request or arguments]
```

$ARGUMENTS
