---
description: "Answer a question in the user's own voice, drawn from their writing style and stated beliefs in the vault. Use to externalize thinking or draft authentic responses."
---

<!-- GENERATED by scripts/ci/generate-commands.js from the same-named skill. Safe to edit: the generator only creates missing commands and never overwrites. -->

# /ghost

Invoke the **`ghost`** skill and apply it to the request below. The full workflow, triggers, and output format live in `skills/ghost/SKILL.md` — read it and follow it.

## Usage

```text
/ghost [request or arguments]
```

$ARGUMENTS
