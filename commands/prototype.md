---
description: "Build a throwaway prototype to answer a design question. Use when the user wants to sanity-check whether a state model or logic feels right, or explore what a UI should look like."
---

<!-- GENERATED by scripts/ci/generate-commands.js from the same-named skill. Safe to edit: the generator only creates missing commands and never overwrites. -->

# /prototype

Invoke the **`prototype`** skill and apply it to the request below. The full workflow, triggers, and output format live in `skills/prototype/SKILL.md` — read it and follow it.

## Usage

```text
/prototype [request or arguments]
```

$ARGUMENTS
