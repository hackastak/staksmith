---
description: "Enforce spec-driven development — settle the requirements, write the spec, break it into tickets, build each one test-first, then gate on the implementation matching the spec. Use when the user says \"let's do this spec-first\", \"spec-driven development\", or invokes /sdd."
---

<!-- GENERATED by scripts/ci/generate-commands.js from the same-named skill. Safe to edit: the generator only creates missing commands and never overwrites. -->

# /sdd-workflow

Invoke the **`sdd-workflow`** skill and apply it to the request below. The full workflow, triggers, and output format live in `skills/sdd-workflow/SKILL.md` — read it and follow it.

## Usage

```text
/sdd-workflow [request or arguments]
```

$ARGUMENTS
