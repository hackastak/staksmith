---
description: "Wire the vault graph — find orphans, missing connections, and cluster bridges, score them, and add links/stubs. Connects notes without generating content. Optional cluster focus."
---

<!-- GENERATED by scripts/ci/generate-commands.js from the same-named skill. Safe to edit: the generator only creates missing commands and never overwrites. -->

Load the skill defined in `skills/backlinks/SKILL.md`, then follow its workflow to handle the request below. Treat that file as the authoritative instructions for this command.

$ARGUMENTS
