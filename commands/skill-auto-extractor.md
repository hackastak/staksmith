---
description: "Mine git history and session logs to automatically create reusable skill definitions from repeated workflows."
---

<!-- GENERATED by scripts/ci/generate-commands.js from the same-named skill. Safe to edit: the generator only creates missing commands and never overwrites. -->

# /skill-auto-extractor

Invoke the **`skill-auto-extractor`** skill and apply it to the request below. The full workflow, triggers, and output format live in `skills/skill-auto-extractor/SKILL.md` — read it and follow it.

## Usage

```text
/skill-auto-extractor [request or arguments]
```

$ARGUMENTS
