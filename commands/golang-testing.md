---
description: "Go testing patterns including table-driven tests, subtests, benchmarks, fuzzing, and test coverage. Follows TDD methodology with idiomatic Go practices."
---

<!-- GENERATED by scripts/ci/generate-commands.js from the same-named skill. Safe to edit: the generator only creates missing commands and never overwrites. -->

# /golang-testing

Invoke the **`golang-testing`** skill and apply it to the request below. The full workflow, triggers, and output format live in `skills/golang-testing/SKILL.md` — read it and follow it.

## Usage

```text
/golang-testing [request or arguments]
```

$ARGUMENTS
