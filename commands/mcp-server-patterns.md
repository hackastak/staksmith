---
description: "Build MCP servers with Node/TypeScript SDK — tools, resources, prompts, Zod validation, stdio vs Streamable HTTP. Use Context7 or official MCP docs for latest API."
---

<!-- GENERATED by scripts/ci/generate-commands.js from the same-named skill. Safe to edit: the generator only creates missing commands and never overwrites. -->

# /mcp-server-patterns

Invoke the **`mcp-server-patterns`** skill and apply it to the request below. The full workflow, triggers, and output format live in `skills/mcp-server-patterns/SKILL.md` — read it and follow it.

## Usage

```text
/mcp-server-patterns [request or arguments]
```

$ARGUMENTS
