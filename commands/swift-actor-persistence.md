---
description: "Thread-safe data persistence in Swift using actors — in-memory cache with file-backed storage, eliminating data races by design."
---

<!-- GENERATED by scripts/ci/generate-commands.js from the same-named skill. Safe to edit: the generator only creates missing commands and never overwrites. -->

# /swift-actor-persistence

Invoke the **`swift-actor-persistence`** skill and apply it to the request below. The full workflow, triggers, and output format live in `skills/swift-actor-persistence/SKILL.md` — read it and follow it.

## Usage

```text
/swift-actor-persistence [request or arguments]
```

$ARGUMENTS
