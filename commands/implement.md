---
description: "Implement a piece of work described by a spec, a set of tickets, a GitHub issue, or a task stated in the conversation. Use when the user says \"implement this\", \"build ticket 03\", or invokes /implement."
---

<!-- GENERATED by scripts/ci/generate-commands.js from the same-named skill. Safe to edit: the generator only creates missing commands and never overwrites. -->

# /implement

Invoke the **`implement`** skill and apply it to the request below. The full workflow, triggers, and output format live in `skills/implement/SKILL.md` — read it and follow it.

## Usage

```text
/implement [request or arguments]
```

$ARGUMENTS
