---
description: "Operate as an agentic engineer using eval-first execution, decomposition, and cost-aware model routing."
---

<!-- GENERATED by scripts/ci/generate-commands.js from the same-named skill. Safe to edit: the generator only creates missing commands and never overwrites. -->

# /agentic-engineering

Invoke the **`agentic-engineering`** skill and apply it to the request below. The full workflow, triggers, and output format live in `skills/agentic-engineering/SKILL.md` — read it and follow it.

## Usage

```text
/agentic-engineering [request or arguments]
```

$ARGUMENTS
