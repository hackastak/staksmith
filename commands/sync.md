---
description: "Load full vault context into Claude Code — recent weekly notes, active projects, master task list, recent inbox, and notes modified in the last 7 days — and output a structured state summary. Use at the start of a work session."
---

<!-- GENERATED by scripts/ci/generate-commands.js from the same-named skill. Safe to edit: the generator only creates missing commands and never overwrites. -->

# /sync

Invoke the **`sync`** skill and apply it to the request below. The full workflow, triggers, and output format live in `skills/sync/SKILL.md` — read it and follow it.

## Usage

```text
/sync [request or arguments]
```

$ARGUMENTS
