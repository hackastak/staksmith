---
description: "Interactive installer for staksmith — guides users through selecting and installing skills and rules to user-level or project-level directories, verifies paths, and optionally optimizes installed files."
---

<!-- GENERATED by scripts/ci/generate-commands.js from the same-named skill. Safe to edit: the generator only creates missing commands and never overwrites. -->

# /configure-staksmith

Invoke the **`configure-staksmith`** skill and apply it to the request below. The full workflow, triggers, and output format live in `skills/configure-staksmith/SKILL.md` — read it and follow it.

## Usage

```text
/configure-staksmith [request or arguments]
```

$ARGUMENTS
