---
description: "Decision framework for choosing between regex and LLM when parsing structured text — start with regex, add LLM only for low-confidence edge cases."
---

<!-- GENERATED by scripts/ci/generate-commands.js from the same-named skill. Safe to edit: the generator only creates missing commands and never overwrites. -->

# /regex-vs-llm-structured-text

Invoke the **`regex-vs-llm-structured-text`** skill and apply it to the request below. The full workflow, triggers, and output format live in `skills/regex-vs-llm-structured-text/SKILL.md` — read it and follow it.

## Usage

```text
/regex-vs-llm-structured-text [request or arguments]
```

$ARGUMENTS
