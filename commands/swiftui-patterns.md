---
description: "SwiftUI architecture patterns, state management with @Observable, view composition, navigation, performance optimization, and modern iOS/macOS UI best practices."
---

<!-- GENERATED by scripts/ci/generate-commands.js from the same-named skill. Safe to edit: the generator only creates missing commands and never overwrites. -->

# /swiftui-patterns

Invoke the **`swiftui-patterns`** skill and apply it to the request below. The full workflow, triggers, and output format live in `skills/swiftui-patterns/SKILL.md` — read it and follow it.

## Usage

```text
/swiftui-patterns [request or arguments]
```

$ARGUMENTS
