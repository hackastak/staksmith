---
description: "Inventory markdown files in your Obsidian vault inbox with basic metadata."
---

<!-- GENERATED by scripts/ci/generate-commands.js from the same-named skill. Safe to edit: the generator only creates missing commands and never overwrites. -->

# /inbox-scan

Invoke the **`inbox-scan`** skill and apply it to the request below. The full workflow, triggers, and output format live in `skills/inbox-scan/SKILL.md` — read it and follow it.

## Usage

```text
/inbox-scan [request or arguments]
```

$ARGUMENTS
