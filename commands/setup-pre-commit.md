---
description: "Set up Husky pre-commit hooks with lint-staged, type checking, and tests in the current repo. Use when the user wants to add pre-commit hooks, set up Husky, configure lint-staged, or add commit-time formatting/typechecking/testing."
---

<!-- GENERATED by scripts/ci/generate-commands.js from the same-named skill. Safe to edit: the generator only creates missing commands and never overwrites. -->

# /setup-pre-commit

Invoke the **`setup-pre-commit`** skill and apply it to the request below. The full workflow, triggers, and output format live in `skills/setup-pre-commit/SKILL.md` — read it and follow it.

## Usage

```text
/setup-pre-commit [request or arguments]
```

$ARGUMENTS
