---
description: "PostgreSQL database patterns for query optimization, schema design, indexing, and security. Based on Supabase best practices."
---

<!-- GENERATED by scripts/ci/generate-commands.js from the same-named skill. Safe to edit: the generator only creates missing commands and never overwrites. -->

# /postgres-patterns

Invoke the **`postgres-patterns`** skill and apply it to the request below. The full workflow, triggers, and output format live in `skills/postgres-patterns/SKILL.md` — read it and follow it.

## Usage

```text
/postgres-patterns [request or arguments]
```

$ARGUMENTS
