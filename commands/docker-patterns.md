---
description: "Docker and Docker Compose patterns for local development, container security, networking, volume strategies, and multi-service orchestration."
---

<!-- GENERATED by scripts/ci/generate-commands.js from the same-named skill. Safe to edit: the generator only creates missing commands and never overwrites. -->

# /docker-patterns

Invoke the **`docker-patterns`** skill and apply it to the request below. The full workflow, triggers, and output format live in `skills/docker-patterns/SKILL.md` — read it and follow it.

## Usage

```text
/docker-patterns [request or arguments]
```

$ARGUMENTS
