---
description: "Anthropic Claude API patterns for Python and TypeScript. Covers Messages API, streaming, tool use, vision, extended thinking, batches, prompt caching, and Claude Agent SDK. Use when building applications with the Claude API or Anthropic SDKs."
---

<!-- GENERATED by scripts/ci/generate-commands.js from the same-named skill. Safe to edit: the generator only creates missing commands and never overwrites. -->

# /claude-api

Invoke the **`claude-api`** skill and apply it to the request below. The full workflow, triggers, and output format live in `skills/claude-api/SKILL.md` — read it and follow it.

## Usage

```text
/claude-api [request or arguments]
```

$ARGUMENTS
