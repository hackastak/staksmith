---
description: "Bun as runtime, package manager, bundler, and test runner. When to choose Bun vs Node, migration notes, and Vercel support."
---

<!-- GENERATED by scripts/ci/generate-commands.js from the same-named skill. Safe to edit: the generator only creates missing commands and never overwrites. -->

# /bun-runtime

Invoke the **`bun-runtime`** skill and apply it to the request below. The full workflow, triggers, and output format live in `skills/bun-runtime/SKILL.md` — read it and follow it.

## Usage

```text
/bun-runtime [request or arguments]
```

$ARGUMENTS
