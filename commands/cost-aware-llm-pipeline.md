---
description: "Cost optimization patterns for LLM API usage — model routing by task complexity, budget tracking, retry logic, and prompt caching."
---

<!-- GENERATED by scripts/ci/generate-commands.js from the same-named skill. Safe to edit: the generator only creates missing commands and never overwrites. -->

# /cost-aware-llm-pipeline

Invoke the **`cost-aware-llm-pipeline`** skill and apply it to the request below. The full workflow, triggers, and output format live in `skills/cost-aware-llm-pipeline/SKILL.md` — read it and follow it.

## Usage

```text
/cost-aware-llm-pipeline [request or arguments]
```

$ARGUMENTS
