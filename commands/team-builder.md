---
description: "Interactive agent picker for composing and dispatching parallel teams"
---

<!-- GENERATED by scripts/ci/generate-commands.js from the same-named skill. Safe to edit: the generator only creates missing commands and never overwrites. -->

# /team-builder

Invoke the **`team-builder`** skill and apply it to the request below. The full workflow, triggers, and output format live in `skills/team-builder/SKILL.md` — read it and follow it.

## Usage

```text
/team-builder [request or arguments]
```

$ARGUMENTS
