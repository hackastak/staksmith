---
description: "Index a project's open todos from the Obsidian vault, prioritize them, and hand back the top 3 next tasks."
---

<!-- GENERATED by scripts/ci/generate-commands.js from the same-named skill. Safe to edit: the generator only creates missing commands and never overwrites. -->

# /what-next

Invoke the **`what-next`** skill and apply it to the request below. The full workflow, triggers, and output format live in `skills/what-next/SKILL.md` — read it and follow it.

## Usage

```text
/what-next [request or arguments]
```

$ARGUMENTS
