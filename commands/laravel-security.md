---
description: "Laravel security best practices for authn/authz, validation, CSRF, mass assignment, file uploads, secrets, rate limiting, and secure deployment."
---

<!-- GENERATED by scripts/ci/generate-commands.js from the same-named skill. Safe to edit: the generator only creates missing commands and never overwrites. -->

# /laravel-security

Invoke the **`laravel-security`** skill and apply it to the request below. The full workflow, triggers, and output format live in `skills/laravel-security/SKILL.md` — read it and follow it.

## Usage

```text
/laravel-security [request or arguments]
```

$ARGUMENTS
