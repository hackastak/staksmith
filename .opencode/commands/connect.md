---
description: "Find connections between two topics through the vault's wikilink graph, or analyze the whole vault for unlinked-but-related notes. Use to discover unexpected relationships between ideas."
---

<!-- GENERATED by scripts/ci/generate-commands.js from the same-named skill. Safe to edit: the generator only creates missing commands and never overwrites. -->

Load the skill defined in `skills/connect/SKILL.md`, then follow its workflow to handle the request below. Treat that file as the authoritative instructions for this command.

$ARGUMENTS
