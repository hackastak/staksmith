---
description: "Turn the current conversation into a spec (a PRD) and publish it to the project's issue tracker — no interview, just synthesis of what has already been discussed. Use when the user says \"turn this into a spec\", \"write the PRD\", or invokes /to-spec."
---

<!-- GENERATED by scripts/ci/generate-commands.js from the same-named skill. Safe to edit: the generator only creates missing commands and never overwrites. -->

Load the skill defined in `skills/to-spec/SKILL.md`, then follow its workflow to handle the request below. Treat that file as the authoritative instructions for this command.

$ARGUMENTS
