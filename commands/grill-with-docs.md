---
description: "A relentless interview that also leaves a paper trail — the glossary and ADRs get written as the decisions crystallise. Use when the user wants to be grilled on a design and have the domain model captured at the same time."
---

<!-- GENERATED by scripts/ci/generate-commands.js from the same-named skill. Safe to edit: the generator only creates missing commands and never overwrites. -->

# /grill-with-docs

Invoke the **`grill-with-docs`** skill and apply it to the request below. The full workflow, triggers, and output format live in `skills/grill-with-docs/SKILL.md` — read it and follow it.

## Usage

```text
/grill-with-docs [request or arguments]
```

$ARGUMENTS
