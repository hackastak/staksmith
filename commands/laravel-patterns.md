---
description: "Laravel architecture patterns, routing/controllers, Eloquent ORM, service layers, queues, events, caching, and API resources for production apps."
---

<!-- GENERATED by scripts/ci/generate-commands.js from the same-named skill. Safe to edit: the generator only creates missing commands and never overwrites. -->

# /laravel-patterns

Invoke the **`laravel-patterns`** skill and apply it to the request below. The full workflow, triggers, and output format live in `skills/laravel-patterns/SKILL.md` — read it and follow it.

## Usage

```text
/laravel-patterns [request or arguments]
```

$ARGUMENTS
