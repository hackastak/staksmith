---
description: "Use when auditing Claude skills and commands for quality. Supports Quick Scan (changed skills only) and Full Stocktake modes with sequential subagent batch evaluation."
---

<!-- GENERATED by scripts/ci/generate-commands.js from the same-named skill. Safe to edit: the generator only creates missing commands and never overwrites. -->

# /skill-stocktake

Invoke the **`skill-stocktake`** skill and apply it to the request below. The full workflow, triggers, and output format live in `skills/skill-stocktake/SKILL.md` — read it and follow it.

## Usage

```text
/skill-stocktake [request or arguments]
```

$ARGUMENTS
