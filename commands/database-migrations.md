---
description: "Database migration best practices for schema changes, data migrations, rollbacks, and zero-downtime deployments across PostgreSQL, MySQL, and common ORMs (Prisma, Drizzle, Django, TypeORM, golang-migrate)."
---

<!-- GENERATED by scripts/ci/generate-commands.js from the same-named skill. Safe to edit: the generator only creates missing commands and never overwrites. -->

# /database-migrations

Invoke the **`database-migrations`** skill and apply it to the request below. The full workflow, triggers, and output format live in `skills/database-migrations/SKILL.md` — read it and follow it.

## Usage

```text
/database-migrations [request or arguments]
```

$ARGUMENTS
