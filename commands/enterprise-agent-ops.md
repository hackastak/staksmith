---
description: "Operate long-lived agent workloads with observability, security boundaries, and lifecycle management."
---

<!-- GENERATED by scripts/ci/generate-commands.js from the same-named skill. Safe to edit: the generator only creates missing commands and never overwrites. -->

# /enterprise-agent-ops

Invoke the **`enterprise-agent-ops`** skill and apply it to the request below. The full workflow, triggers, and output format live in `skills/enterprise-agent-ops/SKILL.md` — read it and follow it.

## Usage

```text
/enterprise-agent-ops [request or arguments]
```

$ARGUMENTS
