---
description: "Pythonic idioms, PEP 8 standards, type hints, and best practices for building robust, efficient, and maintainable Python applications."
---

<!-- GENERATED by scripts/ci/generate-commands.js from the same-named skill. Safe to edit: the generator only creates missing commands and never overwrites. -->

# /python-patterns

Invoke the **`python-patterns`** skill and apply it to the request below. The full workflow, triggers, and output format live in `skills/python-patterns/SKILL.md` — read it and follow it.

## Usage

```text
/python-patterns [request or arguments]
```

$ARGUMENTS
