---
description: "Pattern for progressively refining context retrieval to solve the subagent context problem"
---

<!-- GENERATED by scripts/ci/generate-commands.js from the same-named skill. Safe to edit: the generator only creates missing commands and never overwrites. -->

# /iterative-retrieval

Invoke the **`iterative-retrieval`** skill and apply it to the request below. The full workflow, triggers, and output format live in `skills/iterative-retrieval/SKILL.md` — read it and follow it.

## Usage

```text
/iterative-retrieval [request or arguments]
```

$ARGUMENTS
