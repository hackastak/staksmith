---
description: "Instinct-based learning system that observes sessions via hooks, creates atomic instincts with confidence scoring, and evolves them into skills/commands/agents. v2.1 adds project-scoped instincts to prevent cross-project contamination."
---

<!-- GENERATED by scripts/ci/generate-commands.js from the same-named skill. Safe to edit: the generator only creates missing commands and never overwrites. -->

# /continuous-learning-v2

Invoke the **`continuous-learning-v2`** skill and apply it to the request below. The full workflow, triggers, and output format live in `skills/continuous-learning-v2/SKILL.md` — read it and follow it.

## Usage

```text
/continuous-learning-v2 [request or arguments]
```

$ARGUMENTS
