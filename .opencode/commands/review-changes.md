---
description: "Review only the changes in the working tree — the staged and unstaged diff against HEAD. Read-only, with attribution so inherited debt doesn't block your own work. Use as the tight in-dev loop, or when the user says 'review my changes', 'review the diff', or invokes '/review-changes'."
---

<!-- GENERATED by scripts/ci/generate-commands.js from the same-named skill. Safe to edit: the generator only creates missing commands and never overwrites. -->

Load the skill defined in `skills/review-changes/SKILL.md`, then follow its workflow to handle the request below. Treat that file as the authoritative instructions for this command.

$ARGUMENTS
