---
description: "Research-before-coding workflow. Search for existing tools, libraries, and patterns before writing custom code. Invokes the researcher agent."
---

<!-- GENERATED by scripts/ci/generate-commands.js from the same-named skill. Safe to edit: the generator only creates missing commands and never overwrites. -->

# /search-first

Invoke the **`search-first`** skill and apply it to the request below. The full workflow, triggers, and output format live in `skills/search-first/SKILL.md` — read it and follow it.

## Usage

```text
/search-first [request or arguments]
```

$ARGUMENTS
