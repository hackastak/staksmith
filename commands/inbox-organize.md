---
description: "Move inbox files to their classified destinations."
---

<!-- GENERATED by scripts/ci/generate-commands.js from the same-named skill. Safe to edit: the generator only creates missing commands and never overwrites. -->

# /inbox-organize

Invoke the **`inbox-organize`** skill and apply it to the request below. The full workflow, triggers, and output format live in `skills/inbox-organize/SKILL.md` — read it and follow it.

## Usage

```text
/inbox-organize [request or arguments]
```

$ARGUMENTS
