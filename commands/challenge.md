---
description: "Pressure-test a belief or decision by finding contradictions, hidden assumptions, weak reasoning, and missing perspectives across vault notes. Use before big decisions."
---

<!-- GENERATED by scripts/ci/generate-commands.js from the same-named skill. Safe to edit: the generator only creates missing commands and never overwrites. -->

# /challenge

Invoke the **`challenge`** skill and apply it to the request below. The full workflow, triggers, and output format live in `skills/challenge/SKILL.md` — read it and follow it.

## Usage

```text
/challenge [request or arguments]
```

$ARGUMENTS
