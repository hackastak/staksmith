---
description: "Fetches content from a specified website URL and converts it to markdown format using WebFetch or Playwright for JavaScript-heavy sites. Use when the user wants to extract web page content, convert HTML to markdown, scrape dynamic websites, or save web content locally as markdown files."
---

<!-- GENERATED by scripts/ci/generate-commands.js from the same-named skill. Safe to edit: the generator only creates missing commands and never overwrites. -->

Load the skill defined in `skills/converting-web-to-markdown/SKILL.md`, then follow its workflow to handle the request below. Treat that file as the authoritative instructions for this command.

$ARGUMENTS
