---
description: "Idiomatic Go patterns, best practices, and conventions for building robust, efficient, and maintainable Go applications."
---

<!-- GENERATED by scripts/ci/generate-commands.js from the same-named skill. Safe to edit: the generator only creates missing commands and never overwrites. -->

# /golang-patterns

Invoke the **`golang-patterns`** skill and apply it to the request below. The full workflow, triggers, and output format live in `skills/golang-patterns/SKILL.md` — read it and follow it.

## Usage

```text
/golang-patterns [request or arguments]
```

$ARGUMENTS
