---
description: "Python testing strategies using pytest, TDD methodology, fixtures, mocking, parametrization, and coverage requirements."
---

<!-- GENERATED by scripts/ci/generate-commands.js from the same-named skill. Safe to edit: the generator only creates missing commands and never overwrites. -->

# /python-testing

Invoke the **`python-testing`** skill and apply it to the request below. The full workflow, triggers, and output format live in `skills/python-testing/SKILL.md` — read it and follow it.

## Usage

```text
/python-testing [request or arguments]
```

$ARGUMENTS
