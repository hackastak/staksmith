---
description: "Reference for writing and editing skills well — the vocabulary and principles that make a skill predictable."
---

<!-- GENERATED by scripts/ci/generate-commands.js from the same-named skill. Safe to edit: the generator only creates missing commands and never overwrites. -->

# /skill-design

Invoke the **`skill-design`** skill and apply it to the request below. The full workflow, triggers, and output format live in `skills/skill-design/SKILL.md` — read it and follow it.

## Usage

```text
/skill-design [request or arguments]
```

$ARGUMENTS
