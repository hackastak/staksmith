---
description: "Rust testing patterns including unit tests, integration tests, async testing, property-based testing, mocking, and coverage. Follows TDD methodology."
---

<!-- GENERATED by scripts/ci/generate-commands.js from the same-named skill. Safe to edit: the generator only creates missing commands and never overwrites. -->

# /rust-testing

Invoke the **`rust-testing`** skill and apply it to the request below. The full workflow, triggers, and output format live in `skills/rust-testing/SKILL.md` — read it and follow it.

## Usage

```text
/rust-testing [request or arguments]
```

$ARGUMENTS
