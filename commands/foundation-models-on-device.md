---
description: "Apple FoundationModels framework for on-device LLM — text generation, guided generation with @Generable, tool calling, and snapshot streaming in iOS 26+."
---

<!-- GENERATED by scripts/ci/generate-commands.js from the same-named skill. Safe to edit: the generator only creates missing commands and never overwrites. -->

# /foundation-models-on-device

Invoke the **`foundation-models-on-device`** skill and apply it to the request below. The full workflow, triggers, and output format live in `skills/foundation-models-on-device/SKILL.md` — read it and follow it.

## Usage

```text
/foundation-models-on-device [request or arguments]
```

$ARGUMENTS
