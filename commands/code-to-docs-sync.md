---
description: "Detect drift between code and documentation (READMEs, CLAUDE.md, API docs), propose automated fixes."
---

<!-- GENERATED by scripts/ci/generate-commands.js from the same-named skill. Safe to edit: the generator only creates missing commands and never overwrites. -->

# /code-to-docs-sync

Invoke the **`code-to-docs-sync`** skill and apply it to the request below. The full workflow, triggers, and output format live in `skills/code-to-docs-sync/SKILL.md` — read it and follow it.

## Usage

```text
/code-to-docs-sync [request or arguments]
```

$ARGUMENTS
