---
description: "Automate product packaging for Gumroad - handles guides, templates, skills, frameworks, and bundles. Generates appropriate documentation and sales pages based on product type."
---

<!-- GENERATED by scripts/ci/generate-commands.js from the same-named skill. Safe to edit: the generator only creates missing commands and never overwrites. -->

# /package-product

Invoke the **`package-product`** skill and apply it to the request below. The full workflow, triggers, and output format live in `skills/package-product/SKILL.md` — read it and follow it.

## Usage

```text
/package-product [request or arguments]
```

$ARGUMENTS
