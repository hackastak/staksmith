---
description: "Frontend development patterns for React, Next.js, state management, performance optimization, and UI best practices."
---

<!-- GENERATED by scripts/ci/generate-commands.js from the same-named skill. Safe to edit: the generator only creates missing commands and never overwrites. -->

# /frontend-patterns

Invoke the **`frontend-patterns`** skill and apply it to the request below. The full workflow, triggers, and output format live in `skills/frontend-patterns/SKILL.md` — read it and follow it.

## Usage

```text
/frontend-patterns [request or arguments]
```

$ARGUMENTS
