---
description: "Cache expensive file processing results using SHA-256 content hashes — path-independent, auto-invalidating, with service layer separation."
---

<!-- GENERATED by scripts/ci/generate-commands.js from the same-named skill. Safe to edit: the generator only creates missing commands and never overwrites. -->

# /content-hash-cache-pattern

Invoke the **`content-hash-cache-pattern`** skill and apply it to the request below. The full workflow, triggers, and output format live in `skills/content-hash-cache-pattern/SKILL.md` — read it and follow it.

## Usage

```text
/content-hash-cache-pattern [request or arguments]
```

$ARGUMENTS
