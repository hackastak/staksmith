---
description: "Creates new Claude Agent Skills with proper structure, YAML frontmatter, instructions, and templates. Use when the user wants to create a new skill, generate a SKILL.md file, or scaffold a skill directory."
---

<!-- GENERATED by scripts/ci/generate-commands.js from the same-named skill. Safe to edit: the generator only creates missing commands and never overwrites. -->

# /skill-creator

Invoke the **`skill-creator`** skill and apply it to the request below. The full workflow, triggers, and output format live in `skills/skill-creator/SKILL.md` — read it and follow it.

## Usage

```text
/skill-creator [request or arguments]
```

$ARGUMENTS
