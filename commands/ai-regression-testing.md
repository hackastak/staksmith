---
description: "Regression testing strategies for AI-assisted development. Sandbox-mode API testing without database dependencies, automated bug-check workflows, and patterns to catch AI blind spots where the same model writes and reviews code."
---

<!-- GENERATED by scripts/ci/generate-commands.js from the same-named skill. Safe to edit: the generator only creates missing commands and never overwrites. -->

# /ai-regression-testing

Invoke the **`ai-regression-testing`** skill and apply it to the request below. The full workflow, triggers, and output format live in `skills/ai-regression-testing/SKILL.md` — read it and follow it.

## Usage

```text
/ai-regression-testing [request or arguments]
```

$ARGUMENTS
