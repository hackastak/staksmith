---
description: "Resolve an in-progress git merge or rebase conflict — read the intent behind each side, resolve the hunks, run the project's checks, then hand back for the final commit. Use when a merge or rebase has stopped with conflicts."
---

<!-- GENERATED by scripts/ci/generate-commands.js from the same-named skill. Safe to edit: the generator only creates missing commands and never overwrites. -->

# /resolving-merge-conflicts

Invoke the **`resolving-merge-conflicts`** skill and apply it to the request below. The full workflow, triggers, and output format live in `skills/resolving-merge-conflicts/SKILL.md` — read it and follow it.

## Usage

```text
/resolving-merge-conflicts [request or arguments]
```

$ARGUMENTS
