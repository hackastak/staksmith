---
description: "Idiomatic Rust patterns, ownership, error handling, traits, concurrency, and best practices for building safe, performant applications."
---

<!-- GENERATED by scripts/ci/generate-commands.js from the same-named skill. Safe to edit: the generator only creates missing commands and never overwrites. -->

# /rust-patterns

Invoke the **`rust-patterns`** skill and apply it to the request below. The full workflow, triggers, and output format live in `skills/rust-patterns/SKILL.md` — read it and follow it.

## Usage

```text
/rust-patterns [request or arguments]
```

$ARGUMENTS
