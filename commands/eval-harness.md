---
description: "Formal evaluation framework for Claude Code sessions implementing eval-driven development (EDD) principles"
---

<!-- GENERATED by scripts/ci/generate-commands.js from the same-named skill. Safe to edit: the generator only creates missing commands and never overwrites. -->

# /eval-harness

Invoke the **`eval-harness`** skill and apply it to the request below. The full workflow, triggers, and output format live in `skills/eval-harness/SKILL.md` — read it and follow it.

## Usage

```text
/eval-harness [request or arguments]
```

$ARGUMENTS
