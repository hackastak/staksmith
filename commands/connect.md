---
description: "Find connections between two topics through the vault's wikilink graph, or analyze the whole vault for unlinked-but-related notes. Use to discover unexpected relationships between ideas."
---

<!-- GENERATED by scripts/ci/generate-commands.js from the same-named skill. Safe to edit: the generator only creates missing commands and never overwrites. -->

# /connect

Invoke the **`connect`** skill and apply it to the request below. The full workflow, triggers, and output format live in `skills/connect/SKILL.md` — read it and follow it.

## Usage

```text
/connect [request or arguments]
```

$ARGUMENTS
