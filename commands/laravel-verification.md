---
description: "Verification loop for Laravel projects: env checks, linting, static analysis, tests with coverage, security scans, and deployment readiness."
---

<!-- GENERATED by scripts/ci/generate-commands.js from the same-named skill. Safe to edit: the generator only creates missing commands and never overwrites. -->

# /laravel-verification

Invoke the **`laravel-verification`** skill and apply it to the request below. The full workflow, triggers, and output format live in `skills/laravel-verification/SKILL.md` — read it and follow it.

## Usage

```text
/laravel-verification [request or arguments]
```

$ARGUMENTS
