---
description: "Shared vocabulary for designing deep modules. Use when the user wants to design or improve a module's interface, find deepening opportunities, decide where a seam goes, make code more testable or AI-navigable, or when another skill needs the deep-module vocabulary."
---

<!-- GENERATED by scripts/ci/generate-commands.js from the same-named skill. Safe to edit: the generator only creates missing commands and never overwrites. -->

# /codebase-design

Invoke the **`codebase-design`** skill and apply it to the request below. The full workflow, triggers, and output format live in `skills/codebase-design/SKILL.md` — read it and follow it.

## Usage

```text
/codebase-design [request or arguments]
```

$ARGUMENTS
