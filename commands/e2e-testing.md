---
description: "Playwright E2E testing patterns, Page Object Model, configuration, CI/CD integration, artifact management, and flaky test strategies."
---

<!-- GENERATED by scripts/ci/generate-commands.js from the same-named skill. Safe to edit: the generator only creates missing commands and never overwrites. -->

# /e2e-testing

Invoke the **`e2e-testing`** skill and apply it to the request below. The full workflow, triggers, and output format live in `skills/e2e-testing/SKILL.md` — read it and follow it.

## Usage

```text
/e2e-testing [request or arguments]
```

$ARGUMENTS
