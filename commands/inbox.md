---
description: "Process notes in 0. Inbox/ and organize them into PARA directories with per-file confirmation. Multi-relevance notes go to Resources and are linked from relevant Projects/Areas. Use to clear the inbox."
---

<!-- GENERATED by scripts/ci/generate-commands.js from the same-named skill. Safe to edit: the generator only creates missing commands and never overwrites. -->

# /inbox

Invoke the **`inbox`** skill and apply it to the request below. The full workflow, triggers, and output format live in `skills/inbox/SKILL.md` — read it and follow it.

## Usage

```text
/inbox [request or arguments]
```

$ARGUMENTS
