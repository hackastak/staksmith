---
description: "Manage The HackaStak content calendar and publishing pipeline — buffer health, pillar balance, scheduling, and publish tracking. Use to see status or move articles through the pipeline."
---

<!-- GENERATED by scripts/ci/generate-commands.js from the same-named skill. Safe to edit: the generator only creates missing commands and never overwrites. -->

# /content

Invoke the **`content`** skill and apply it to the request below. The full workflow, triggers, and output format live in `skills/content/SKILL.md` — read it and follow it.

## Usage

```text
/content [request or arguments]
```

$ARGUMENTS
