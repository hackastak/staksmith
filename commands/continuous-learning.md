---
description: "Automatically extract reusable patterns from Claude Code sessions and save them as learned skills for future use."
---

<!-- GENERATED by scripts/ci/generate-commands.js from the same-named skill. Safe to edit: the generator only creates missing commands and never overwrites. -->

# /continuous-learning

Invoke the **`continuous-learning`** skill and apply it to the request below. The full workflow, triggers, and output format live in `skills/continuous-learning/SKILL.md` — read it and follow it.

## Usage

```text
/continuous-learning [request or arguments]
```

$ARGUMENTS
