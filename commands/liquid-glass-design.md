---
description: "iOS 26 Liquid Glass design system — dynamic glass material with blur, reflection, and interactive morphing for SwiftUI, UIKit, and WidgetKit."
---

<!-- GENERATED by scripts/ci/generate-commands.js from the same-named skill. Safe to edit: the generator only creates missing commands and never overwrites. -->

# /liquid-glass-design

Invoke the **`liquid-glass-design`** skill and apply it to the request below. The full workflow, triggers, and output format live in `skills/liquid-glass-design/SKILL.md` — read it and follow it.

## Usage

```text
/liquid-glass-design [request or arguments]
```

$ARGUMENTS
