---
description: "Diagnosis loop for hard bugs and performance regressions. Use when the user says \"diagnose\"/\"debug this\", or reports something broken/throwing/failing/slow."
---

<!-- GENERATED by scripts/ci/generate-commands.js from the same-named skill. Safe to edit: the generator only creates missing commands and never overwrites. -->

# /diagnosing-bugs

Invoke the **`diagnosing-bugs`** skill and apply it to the request below. The full workflow, triggers, and output format live in `skills/diagnosing-bugs/SKILL.md` — read it and follow it.

## Usage

```text
/diagnosing-bugs [request or arguments]
```

$ARGUMENTS
