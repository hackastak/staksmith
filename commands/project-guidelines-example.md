---
description: "Example project-specific skill template based on a real production application."
---

<!-- GENERATED by scripts/ci/generate-commands.js from the same-named skill. Safe to edit: the generator only creates missing commands and never overwrites. -->

# /project-guidelines-example

Invoke the **`project-guidelines-example`** skill and apply it to the request below. The full workflow, triggers, and output format live in `skills/project-guidelines-example/SKILL.md` — read it and follow it.

## Usage

```text
/project-guidelines-example [request or arguments]
```

$ARGUMENTS
