---
description: "Test-driven development for Laravel with PHPUnit and Pest, factories, database testing, fakes, and coverage targets."
---

<!-- GENERATED by scripts/ci/generate-commands.js from the same-named skill. Safe to edit: the generator only creates missing commands and never overwrites. -->

# /laravel-tdd

Invoke the **`laravel-tdd`** skill and apply it to the request below. The full workflow, triggers, and output format live in `skills/laravel-tdd/SKILL.md` — read it and follow it.

## Usage

```text
/laravel-tdd [request or arguments]
```

$ARGUMENTS
