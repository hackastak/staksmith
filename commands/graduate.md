---
description: "Extract undeveloped ideas from weekly notes and promote them into standalone seedling notes in 0. Inbox/Graduates/. Use to capture half-formed thoughts before they're lost."
---

<!-- GENERATED by scripts/ci/generate-commands.js from the same-named skill. Safe to edit: the generator only creates missing commands and never overwrites. -->

# /graduate

Invoke the **`graduate`** skill and apply it to the request below. The full workflow, triggers, and output format live in `skills/graduate/SKILL.md` — read it and follow it.

## Usage

```text
/graduate [request or arguments]
```

$ARGUMENTS
